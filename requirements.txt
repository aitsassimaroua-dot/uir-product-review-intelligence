# Core
python-dotenv>=1.0.0
pydantic>=2.5.0

# Agents
crewai>=0.80.0
crewai-tools>=0.14.0

# LLM backends (LiteLLM handles routing under CrewAI)
google-generativeai>=0.8.0

# Deep learning (DL model = fine-tuned BERT)
torch>=2.0.0
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.27.0
scikit-learn>=1.3.0

# Data + plotting
pandas>=2.0.0
numpy>=1.26.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Web search tool (free — DuckDuckGo)
duckduckgo-search>=6.0.0

# Logging
python-json-logger>=2.0.7

# UI (optional — for `streamlit run streamlit_app.py`)
streamlit>=1.32.0

# Tests
pytest>=8.0.0
pytest-cov>=5.0.0
